= simple_form_for @band do |f|
  = f.input :name
  = f.association :genre
  = f.input :description
  = f.input :links, label: 'Input comma-sparated links', input_html: { value: f.object.links.try(:join, ', ') }
  = f.hidden_field :form_id, value: 111 
  = f.button :submit
