- shows_by_genres.each do |genre, shows|
  -#- next if shows.empty?
  %table.table.pull-left{ style: 'width: 350px; margin: 20px;'}
    %thead
      %tr
        %th=genre
    - shows.each do |show|
      %tr
        %td
          = show.humanized_time
          = link_to show.band.name, show.band
          at
          = link_to show.venue.name, show.venue
          - if show.band.songs.primary
            %audio{ controls: 'true' }
              %source{ src: show.band.songs.primary.upload.url }
          - else
            -
