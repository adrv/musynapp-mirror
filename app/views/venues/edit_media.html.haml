%h2 Upload an image
= render partial: 'shared/media_uploader', locals: { media_type: 'images', object: @venue }
%h2 Upload a video
= render partial: 'shared/media_uploader', locals: { media_type: 'videos', object: @venue }
